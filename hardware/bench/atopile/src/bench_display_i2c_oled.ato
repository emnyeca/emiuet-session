# Display option A: 128x64 I2C OLED, as a generic 4-pin connector.
# Wiring intent only (SDA / SCL / VCC / GND).

import I2C, Power from "connectors.ato"

module BenchDisplayI2cOled:
    i2c = new I2C
    power = new Power
