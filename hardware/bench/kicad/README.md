# bench/kicad (reserved)

Reserved for later **bench carrier** boards (Phase E), e.g. a small board that
holds the Teensy 4.1 and the replaceable key/display sections.

This is separate from the existing root [`kicad/phase1-testboard/`](../../../kicad/),
which is the earlier analog breadboard test board.

Empty in Phase A — kept so the directory is tracked by git.

ここは後段（Phase E）の **ベンチ用キャリア基板** 置き場です。Phase A では空です。
