# EC11-style push rotary encoder for the Phase A bench.
# Wiring intent only: quadrature A/B plus a momentary push switch.

import Power from "connectors.ato"

module BenchEncoder:
    signal a    # quadrature A
    signal b    # quadrature B
    signal sw   # push switch
    power = new Power
